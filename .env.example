# Frontend Configuration
NODE_ENV=development

# JWT Secret (used by API routes)
JWT_SECRET=your-secret-key-change-in-production

# External Sensor API (optional - defaults to public API)
EXTERNAL_SENSOR_API=https://sensores-async-api.onrender.com/api/sensors/all

# Microservices URLs (for production deployment)
# NEXT_PUBLIC_AUTH_SERVICE_URL=http://localhost:3001
# NEXT_PUBLIC_PARCEL_SERVICE_URL=http://localhost:3002
# NEXT_PUBLIC_INGESTION_SERVICE_URL=http://localhost:3003

# For local development without microservices, the frontend uses API routes
# and does not require these environment variables
